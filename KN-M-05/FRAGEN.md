# Fragen – KN-M-05 (MongoDB: Administration, Backup & Skalierung)

1. Warum schlägt der Login fehl, wenn **authSource falsch** ist?
2. Was ist der Unterschied zwischen der Rolle **`read`** und **`readWrite`**?
3. Warum sollte man **keine "Any"-Rollen** verwenden? (`readAnyDatabase` etc.)
4. Was ist der Unterschied zwischen **EBS Snapshot** und **mongodump**?
5. Warum muss der **EBS Snapshot in der gleichen Availability Zone** sein?
6. Was passiert beim Restore via **EBS Snapshot** mit dem alten Volume?
7. Was ist ein **Replica Set**?
8. Wann verwendet man **Replikation**, wann **Sharding**?
9. Was ist der Unterschied zwischen **vertikaler** und **horizontaler** Skalierung?
10. Wie erstellt man **Benutzer mit spezifischen Rechten** in MongoDB?
11. Wie testet man, ob ein Benutzer **keine Schreibrechte** hat?
12. Was ist **mongodump** / **mongorestore**?

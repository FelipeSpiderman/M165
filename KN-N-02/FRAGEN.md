# Fragen – KN-N-02 (Neo4j: Datenabfrage & -Manipulation)

1. Was macht **OPTIONAL MATCH** genau?
2. Was passiert, wenn man **DELETE ohne DETACH** verwendet?
3. Was ist der Unterschied zwischen **SET** und **MERGE**?
4. Wie funktioniert **CASE in Cypher**?
5. Wie filtert man mit **WHERE auf Kanten-Attribute**?
6. Was macht **`collect()`** in Cypher?
7. Wie funktioniert **MERGE**? Unterschied zu CREATE?
8. Was ist der Unterschied zwischen **MATCH und OPTIONAL MATCH**?
9. Was macht **DETACH DELETE**?
10. Wie erstellt man in Cypher **bedingte Logik**? (`CASE` / `WHEN` / `THEN` / `ELSE`)
11. Wie erstellt man mit einem einzigen **CREATE** mehrere Knoten und Kanten?
12. Wie filtert man mit der **`IN`**-Klausel in Cypher?

# Fragen – KN-M-03 (MongoDB: Datenmanipulation & Abfragen I)

1. Was ist der Unterschied zwischen `insertOne()` und `insertMany()`?
2. Warum verwendet man `new ObjectId()` statt festen IDs?
3. Was ist der Unterschied zwischen `drop()` und `deleteMany({})`?
4. Wie löscht man mit **ODER-Verknüpfung** in MongoDB? (`$or`)
5. Wie filtert man mit **UND** in MongoDB? (`$and`)
6. Wie funktioniert **Regex** in MongoDB? (`$regex`, `$options: "i"`)
7. Was ist der Unterschied zwischen **Projektion mit `_id`** und **ohne `_id`**?
8. Was ist der Unterschied zwischen `updateOne()` und `replaceOne()`?
9. Was macht **`$set`** beim Update?
10. Was macht **`$inc`** beim Update?
11. Wie filtert man nach einem **DateTime**-Feld?
12. Was ist der Unterschied zwischen `updateOne()` mit `$set` und `replaceOne()` – was passiert mit den nicht-genannten Feldern?

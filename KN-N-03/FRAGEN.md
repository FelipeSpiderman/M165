# Fragen – KN-N-03 (Neo4j: Programmierung mit Python)

1. Welche **Library** verwendest du für Neo4j in Python? (`neo4j`)
2. Wie unterscheidet sich der **neo4j Driver von pymongo**?
3. Was sind **Sessions und Transactions** im neo4j Driver?
4. Wie verhindert man **Cypher-Injection**? (`$param`-Syntax)
5. Warum **`driver.close()`** am Ende?
6. Wie verbindet man sich von Python zu Neo4j?
7. Was ist der Unterschied zwischen **`execute_read`** und **`execute_write`**?
8. Wie übergibt man **Parameter** in Cypher-Queries in Python?
9. Wie führt man einen **Join über Beziehungen** in Neo4j mit Python durch?
10. Wie macht man **Aggregationen** in Cypher über Python?
11. Wie gibt man **Records** aus einer Neo4j-Abfrage in Python aus?

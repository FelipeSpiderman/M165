# Fragen – KN-M-07 (MongoDB: Programmierung mit Python)

1. Welche **Library** verwendest du für MongoDB in Python? (`pymongo`)
2. Was ist der Unterschied zwischen **`find_one()`** und **`find()`**?
3. Wie macht man ein **Datum in Python** für MongoDB? (`datetime`-Objekt)
4. Wie schreibt man eine **Aggregations-Pipeline in Python**?
5. Was gibt **`insert_one()`** zurück? (`inserted_id`)
6. Warum **`client.close()`** am Ende?
7. Wie verbindest du dich mit **Authentifizierung** von Python zu MongoDB?
8. Wie setzt du **Filter und Projektion** in Python um?
9. Wie führst du **`$lookup`** mit Python aus?
10. Wie unterscheidet sich die **Python-Syntax von mongosh**?
11. Welche **Sicherheitsmassnahme** beachtest du bei Passwörtern im Code?

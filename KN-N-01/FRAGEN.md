# Fragen – KN-N-01 (Neo4j: Installation & Graph-Modellierung)

1. Wie unterscheidet sich das **Neo4j-Modell vom MongoDB-Modell**?
2. Warum hast du das **Attribut auf die Kante** und nicht auf den Knoten gelegt?
3. Was sind **Labels** in Neo4j?
4. Welche Tools gibt es für die **Graph-Modellierung**? (draw.io, arrows.app)
5. Wie löst Neo4j eine **N:N-Beziehung**?
6. Wie installiert man Neo4j via **Cloud-Init auf AWS**?
7. Was ist der **Standard-Login** für Neo4j?
8. Welche **Ports** verwendet Neo4j? (7687 Bolt, 7474 HTTP)
9. Was ist **cypher-shell**?
10. Welche **Knoten-Labels** hast du definiert und warum?
11. Welche **Kantentypen** hast du modelliert? (`SPIELT_FUER`, `TRAINIERT`, `HAT_GESPIELT`)

# Fragen – KN-C-01 (Cassandra: Installation & Datenmodellierung)

1. Was ist der Unterschied zwischen **Partition Key** und **Cluster Key**?
2. Warum werden Daten in Cassandra **redundant** gespeichert?
3. Was ist ein **Keyspace**?
4. Warum gibt es in Cassandra **keine Joins**?
5. Wie wählt man einen **guten Partition Key** aus?
6. Was passiert, wenn der Partition Key **zu viele Daten** hat? (Hot Spot Problem)
7. Wie unterscheidet sich das Cassandra-Datenmodell vom relationalen Modell?
8. Warum modelliert man bei Cassandra nach **Screens** (Abfrage-Szenarien) und nicht nach Entitäten?
9. Was ist der Unterschied zwischen `SimpleStrategy` und `NetworkTopologyStrategy` bei der Replikation?
10. Wie installiert man Cassandra via **Docker**?
11. Was ist **cqlsh**?
12. Welche Cassandra-Datentypen kennst du? (`text`, `int`, `double`, `date`, `set<text>`)
